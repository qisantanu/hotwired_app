<li>
  <p><%= notification.title %></p>
  <span class='desc'><%= notification.description %></span>
  <span class='time'>Time: <%= notification.created_at.strftime('%D %a at %I:%M%p') %></span>
</li>
